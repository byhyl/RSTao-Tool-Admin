# RSTao-Tool 授权管理指南

## 一、整体架构

```
┌─────────────────────────────────────────────────────┐
│              你的电脑（管理员 / 卖家）                  │
│                                                     │
│   admin_tool.py  ──启动──▶  激活服务器 (内置)          │
│       │                         │                   │
│       │  生成激活码               │  验证激活请求       │
│       │  管理黑名单               │  下发许可密钥       │
│       │  查看激活记录             │  SQLite 数据库      │
│       ▼                         ▼                   │
│   ┌──────────────────────────────────────────┐      │
│   │          server/activation.db              │      │
│   │  激活码库 │ 激活记录 │ 黑名单 │ 令牌        │      │
│   └──────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────┘
                         │
                         │ 客户输入激活码，连你的服务器
                         ▼
┌─────────────────────────────────────────────────────┐
│              客户的电脑（买家 / 用户）                  │
│                                                     │
│   main.py  ──启动──▶  激活窗口                        │
│                         │                           │
│                   选择「在线激活码激活」                 │
│                   输入你给的激活码                     │
│                         │                           │
│                   服务器验证通过                       │
│                   自动保存许可文件                     │
│                   软件正常使用                         │
└─────────────────────────────────────────────────────┘
```

---

## 二、启动服务器

### 什么是激活服务器？

激活服务器是一个运行在你电脑上的小程序，它做三件事：

1. **验证激活码**：客户输入激活码后，客户端发请求到你的服务器，服务器判断这个码是否有效
2. **下发许可**：验证通过后，服务器生成加密的许可密钥返回给客户端
3. **记录留痕**：每次激活都会被记录，谁、什么设备、什么时候

**服务器不启动，在线激活码功能就无法使用。** 离线密钥生成不受影响。

### 如何启动？

1. 在 VSCode 中按 `F5`，选择「🔑 授权管理中心」
2. 切换到「服务器配置」标签页
3. 点击 **启动服务器** 按钮
4. 等待状态变为 `● 运行中`，日志显示「服务器启动成功」

> 💡 勾选「启动工具时自动开启服务器」，以后打开管理工具就自动启动了。

### 服务器端口

默认监听 `18080` 端口。如果被占用，改成其他端口（如 `18081`），同时记得修改「服务器地址」保持一致。

---

## 三、给用户发激活码（完整流程）

### 第一步：生成激活码

1. 在「服务器配置」页确认服务器已启动（`● 运行中`）
2. 切换到「在线激活码管理」标签页
3. 在左侧面板设置：

| 参数 | 说明 | 举例 |
|------|------|------|
| 类型 | `permanent` = 永久 / `days` = 按天数 / `date` = 指定日期 | 选 `permanent` |
| 可激活次数 | 这个码能用几次（通常设 1） | `1` |
| 备注 | 记录是哪个客户，方便后续管理 | `张三-XX公司` |

4. 点击 **生成激活码**
5. 弹窗显示激活码（例如 `3C640C0AFC89FCAD`），自动复制到剪贴板

### 第二步：发送给用户

把激活码发给客户（微信、邮件等），同时告知：

```
激活码：3C640C0AFC89FCAD
使用方法：
1. 打开 RSTao-Tool 软件
2. 在激活窗口选择「在线激活码激活」
3. 粘贴激活码，点击「激活软件」
4. 确保电脑能连接互联网
```

### 第三步：用户激活后

- 激活记录会自动出现在右侧「激活记录」页
- 可以看到激活时间、设备指纹
- 用户下次打开软件会自动验证，无需重复激活

---

## 四、服务器管理操作

所有操作都在「在线激活码管理」标签页完成。

### 查看激活码列表

右侧「激活码列表」标签页显示所有生成过的激活码：

```
🟢 有效  /  🔴 已作废
类型、已用次数/最大次数、备注
```

### 作废激活码

1. 点击「作废选中」按钮
2. 输入要作废的激活码
3. 确认后该激活码立即失效，任何人都无法再用

### 查看激活记录

右侧「激活记录」标签页显示所有成功的激活：

```
时间 | 激活码 | 设备指纹
```

### 黑名单管理

如果某个客户违规使用（如多次激活不同设备），可以拉黑：

1. 从「激活记录」中找到他的设备指纹
2. 在左侧「黑名单管理」输入框中粘贴设备指纹
3. 点击 **加入黑名单**

拉黑后：
- 该设备无法再激活任何码
- 已激活的设备不受影响（只能阻止新激活）

如需解封，输入设备指纹后点击 **移除**。

---

## 五、两种授权方式对比

| | 离线密钥 | 在线激活码 |
|---|---|---|
| **使用场景** | 内网客户、无网络环境 | 互联网客户 |
| **操作方式** | 用户提供机器码，你生成密钥发回 | 你生成短码发给用户即可 |
| **需要服务器** | ❌ 不需要 | ✅ 需要启动 |
| **远程管控** | ❌ 无法作废/拉黑 | ✅ 随时作废、拉黑 |
| **使用次数限制** | ❌ 不限 | ✅ 可设最大次数 |
| **操作标签页** | 「离线密钥生成」 | 「在线激活码管理」 |

---

## 六、数据安全

### 数据存在哪？

所有数据存储在 `server/activation.db`（SQLite 文件），位于项目 `server/` 目录下。

### 如何备份？

直接复制 `server/activation.db` 文件即可。建议定期备份。

### 管理员令牌

- 令牌具有完全管理权限，**请务必保管好**
- 已保存在 `.admin_config.json` 中，不要泄露此文件
- 如需更换令牌，删除 `server/activation.db` 重新生成（**注意：会清空所有数据**）

### 配置文件

`.admin_config.json` 保存了服务器地址和令牌，位于项目根目录。不要提交到 Git 或发给他人。

---

## 七、公网部署（给外网客户使用）

当前服务器只能本机访问。如果要让互联网上的客户激活：

### 方案一：云服务器部署（推荐）

1. 购买一台云服务器（阿里云/腾讯云轻量服务器，几十元/月）
2. 把整个 `server/` 目录上传到服务器
3. 服务器上安装 Python 和 pycryptodome：`pip install pycryptodome`
4. 开放防火墙 18080 端口
5. 运行 `python activation_server.py`
6. 在管理工具「服务器配置」中把地址改为云服务器 IP

### 方案二：内网穿透

使用 frp / ngrok 等工具将本机端口暴露到公网，适合测试阶段。

---

## 八、常见问题

**Q: 服务器启动失败？**
- 检查端口是否被占用（改个端口试试）
- 检查是否安装了 pycryptodome：`pip install pycryptodome`

**Q: 客户说激活失败？**
- 确认服务器在运行中
- 确认客户能访问到你的服务器地址
- 查看「激活码列表」确认该码是否还有剩余次数

**Q: 如何限制客户只能在特定日期前使用？**
- 生成激活码时类型选 `date`，设置过期时间

**Q: 换了电脑，数据能迁移吗？**
- 复制 `server/activation.db` 到新电脑即可

**Q: 不小心删了数据库怎么办？**
- 已激活的客户不受影响（许可已保存在他们本地）
- 丢失的是激活码库和记录，建议定期备份